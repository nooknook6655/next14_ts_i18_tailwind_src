/** @type {import('next').NextConfig} */
const nextConfig = {
    "compilerOptions": {
    "moduleResolution": "node"
  },
  "include": [
    "src/**/*",
    "node_modules"
  ]
};

export default nextConfig;
